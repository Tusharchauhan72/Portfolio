# GitHub Personal Access Token (PAT) for GraphQL API
# Generate at: https://github.com/settings/tokens
# Required scopes: read:user, user:email (or repo scope if private contributions are included)
GITHUB_TOKEN=your_github_personal_access_token_here
